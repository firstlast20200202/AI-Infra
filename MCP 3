building MCP server in python: refer to the project in repo MCP-STDIO
choice 1: STDIO
in terminal: 
$ curl -LsSf http://astral.sh/uv/install.sh | sh        //install uv
$ uv init .
$ uv venv        //create virtual environment
copy 'source .venv/bin/activate' in output and run $ source .venv/bin/activate
$ python main.py       //verify

open the project in VScode which has been initialised above, update main.py as below:
from mcp.server.fastmcp import FastMCP        //library

mcp = FastMCP("add_integers")

@mcp.tool()
def add_integers(a: int, b: int) -> int:
    """
    Adds two integers and returns the sum.

    Args:
        a: First integer
        b: Second integer

    Returns:
        The sum of a and b.
    """
    return a + b

if __name__ == "__main__":
    mcp.run(transport="stdio")

terminal in VScode: 
$ uv add "mcp[cli]"               //set up this as a MCP server that runs from the command line
$ uv run mcp dev main.py          //run a developer server, it opens a browser page where MCP server is running to test tools
click Tools, it will show 'add_ integers' tool in the list, click 'add_ integers', it will show the tool where we can fill in value a and b to test the tool.

other tools added besides add_integers:
add some error checking
simulate a long-running process
set up logging (it would generate a mcp_server.log file in the project once test this tool, actually it didnt...2333)



building MCP server in node.js: refer to the project in repo MCP-STDIO-JS
terminal in VScode:
$ npm init -y
$ npm install @modelcontextprotocol/sdk
create a file server.js with contents.

testing in different ways:
1. open terminal in VScode and run commands below, it would show result in json in output:
$ echo '{"jsonrpc":"2.0","id":1,"method":"tools/list","param":{}}' | node server.js
$ echo '{"jsonrpc":"2.0","id":1,"method":"tools/list","param":{{"name": "add-integers", "arguments": { "a": 3, "b": 5 }}}' | node server.js
2. in Postman create a MCP request, and input 'node + <copy path of server.js in VScode>' as STDIO url, then connect, it would list available tools like 'add-integers'.
3. open terminal in VScode and run $ npx @modelcontextprotocol/inspector node server.js, it would open a browser page which is pretty much the same as the first way







